# MongoDB for Order Service
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-orders
  namespace: order-management
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongodb-orders
  template:
    metadata:
      labels:
        app: mongodb-orders
    spec:
      containers:
      - name: mongodb
        image: mongo:6.0
        ports:
        - containerPort: 27017
        env:
        - name: MONGO_INITDB_DATABASE
          value: "orders"
        volumeMounts:
        - name: mongodb-storage
          mountPath: /data/db
      volumes:
      - name: mongodb-storage
        persistentVolumeClaim:
          claimName: mongodb-orders-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mongodb-orders-service
  namespace: order-management
spec:
  selector:
    app: mongodb-orders
  ports:
  - port: 27017
    targetPort: 27017
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-orders-pvc
  namespace: order-management
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi